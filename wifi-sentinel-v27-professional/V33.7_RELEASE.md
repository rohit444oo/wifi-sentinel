# V33.7 — Client Visibility & Evidence-Based Rogue Assessment

## Added
- AP investigation now exposes LAN-visible client inventory using real `network_devices` telemetry.
- Client records show hostname, IP, MAC, vendor and last observed time.
- The UI explicitly distinguishes LAN-visible client telemetry from per-AP association telemetry.
- AP investigation includes a structured detection decision chain using observed authorization, SSID impersonation, security exposure, baseline deviation, behavioral deviation and profile consistency indicators.
- Detection output remains evidence-based; no synthetic client names, locations, risk values or live APs are generated.
- Executive PDF reporting includes a client-visibility section with the same real LAN telemetry and an explicit association-scope note.

## Preserved
- Scanner, radar, security posture layout, historical AP handling, reporting flow, database schema and existing UI structure were otherwise preserved.

## Verification
- Python compilation: PASS
- Client helper import/compile: PASS
- Full pytest suite: NOT RUNNABLE in the build environment because Flask is not installed; no claim of runtime test is made.
